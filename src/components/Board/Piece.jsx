// // Piece.js
// import React from "react";

// const Piece = ({ type, color }) => {
//   const getPieceImagePath = (pieceType, pieceColor) => {
//     const colorPrefix = pieceColor === "white" ? "w" : "b";
//     return require(`./assets/${colorPrefix}${pieceType}.png`).default;
//   };

//   return (
//     <img
//       className="piece"
//       src={getPieceImagePath(type, color)}
//       alt={`${color} ${type}`}
//     />
//   );
// };

// export default Piece;
